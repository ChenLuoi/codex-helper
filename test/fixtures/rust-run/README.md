# rust-run fixture

Committed fixture data for the local JS/Rust comparison harness.

The data is synthetic. It must not be replaced with real `~/.codex/sessions`
content, real account identifiers, or real tokens.
